# Munshi Dada Phase 1 — ML / Intent Classification Service
# Copy to .env and fill in real values:
#   cp .env.example .env
#
# Loaded by python-dotenv in bot_engine.py on startup.

# ---------------------------------------------------------------------------
# REQUIRED — application runtime
# ---------------------------------------------------------------------------

# OpenAI API key used for:
#   - Intent classification (/classify)
#   - General chat fallback replies
#   - WhatsApp message conversion (/convert)
OPENAI_API_KEY="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"

# ---------------------------------------------------------------------------
# NOT READ BY CODE TODAY (hardcoded in main.py / bot_engine.py / Dockerfile)
# Documented here for local ops reference only.
# ---------------------------------------------------------------------------

# API bind host (hardcoded: 0.0.0.0 in main.py and Dockerfile CMD)
# HOST=0.0.0.0

# API port (hardcoded: 8000 in main.py and Dockerfile CMD)
# PORT=8000

# OpenAI chat model (hardcoded: gpt-4.1-mini in bot_engine.py)
# CHAT_MODEL=gpt-4.1-mini

# ---------------------------------------------------------------------------
# Docker / CI only (set in Dockerfile or GitHub Actions, not in .env)
# ---------------------------------------------------------------------------

# PYTHONUNBUFFERED=1
# PYTHONDONTWRITEBYTECODE=1
# PIP_NO_CACHE_DIR=1
# PIP_DISABLE_PIP_VERSION_CHECK=1
# PYTHONPATH=/app

# GitHub Actions deployment secrets (not app .env):
# DOCKER_PASSWORD, EC2_HOST, EC2_SSH_KEY

# ---------------------------------------------------------------------------
# Backend integration (Munshi NestJS — separate repo)
# Backend points to this service via ML_URL, e.g.:
#   ML_URL=http://localhost:8000
# ---------------------------------------------------------------------------
