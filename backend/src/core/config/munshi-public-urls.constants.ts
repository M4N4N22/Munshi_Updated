/** Production web app (Vercel + custom domain). */
export const MUNSHI_PUBLIC_WEB_ORIGIN = 'https://www.munshidada.com';

/** Owner onboarding / registration (OTP + factory signup). */
export const MUNSHI_ONBOARDING_URL = `${MUNSHI_PUBLIC_WEB_ORIGIN}/onboarding`;
